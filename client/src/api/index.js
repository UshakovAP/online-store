import products from './fake.api/products.api';
import categories from './fake.api/categories.api';
import basket from './fake.api/basket.api';

const api = {
    products,
    categories,
    basket,
};

export default api;
